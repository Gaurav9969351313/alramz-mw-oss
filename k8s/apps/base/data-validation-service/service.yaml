apiVersion: v1
kind: Service
metadata:
  name: data-validation-service
  labels:
    app: data-validation-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: data-validation-service
  type: ClusterIP
