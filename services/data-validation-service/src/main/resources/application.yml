server:
  host: localhost

spring:
  application:
    name: data-validation-service
  profiles:
    active: dev
  jpa:
    hibernate:
      ddl-auto: validate
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

sql:
  file: classpath:sql/sql-queries.xml
