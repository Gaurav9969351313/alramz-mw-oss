spring:
  liquibase:
    change-log: classpath:db/changelog/preprod/db.changelog-master.yaml

server:
  address: 0.0.0.0
  port: 8080
  host: localhost

management:
  endpoints:
    web:
      exposure:
        include: "*"

cipher:
  password: UseMostSecretKey

company:
  redis:
    deployment:
      mode: local
    connection:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      ssl:
        enabled: ${REDIS_SSL_ENABLED:false}
      timeout: ${REDIS_TIMEOUT:2s}
    cache:
      mappings:
        global-config:
          table: global_configuration_settings
          cache-key: alramz:reference-data:v1:global-config
          reload-strategy: explicit
          ttl: ${CACHE_TTL:3600s}
          enabled: true
  scheduler:
    pool-size: 5
    thread-group-name: reference-data-scheduled-tasks-
    thread-name-prefix: ReferenceData-TaskScheduler-Worker-
  logging:
    json:
      enabled: false
      pretty-print: false
    correlation-id:
      response-header: true
    request:
      enabled: true
      include-headers: true
      include-payload: true
    response:
      enabled: true
      include-payload: true
    aspect:
      enabled: true
    performance:
      threshold: 1000ms
    database-logging:
      enabled: true
      include-request-payload: true
      include-response-payload: true
      cleanup-cron: "0 0 2 * * *"
      retention-days: 7
      excluded-paths:
        - /api/v1/info
        - /actuator/**
        - /swagger-ui/**
        - /v3/api-docs/**
    seq:
      enabled: true
  jwt:
    enabled: true
    secret: ${JWT_SECRET:defaultSecretKeyChangeMeInProduction1234567890}
    login-url: /api/auth/login
    register-url: /api/auth/register
    refresh-url: /api/auth/refresh
    logout-url: /api/auth/logout
    validate-url: /api/auth/validate
    permit-all-urls:
      - /api/v1/info
